LLM_PROVIDER=ollama
LLM_MODEL=llama3.2:3b

# If LLM_PROVIDER=openai instead:
# OPENAI_API_KEY=sk-...
# LLM_MODEL=gpt-4o-mini

# Optional — Langfuse observability (skipped cleanly if unset, never required):
# LANGFUSE_PUBLIC_KEY=pk-...
# LANGFUSE_SECRET_KEY=sk-...
